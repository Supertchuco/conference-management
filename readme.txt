Conference Management Project

This web application was created to help to managemnt the 


This web project that use these frameworks:
	- SpringBoot
	- Angular

To run this project you have two options:
 - Import the project in Intellij ide and running the major class ''
 - Open a terminal and inside project path  and execute o command to build the application
 'gradlew build', after  access this path inside the project: 'build/lib' and
 execute the command: 'java -jar conference-management.jar'
 
In both of aproaches you can access the application through browser and this url
http://localhost:8080
	